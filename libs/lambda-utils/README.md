# lambda-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build lambda-utils` to build the library.

## Running unit tests

Run `nx test lambda-utils` to execute the unit tests via [Jest](https://jestjs.io).
